jar {
    jar.enabled(false)
}

// Clean all projects
task deepClean {
    dependsOn rootProject.allprojects.collect({ it.tasks.findByName('clean') })
}

/*task assembleProduction {
    dependsOn this.deepClean
    dependsOn rootProject.project(':Tool').build
    dependsOn rootProject.project(':Component').build
    dependsOn rootProject.project(':Game').build

    dependsOn rootProject.copyJars

}*/ // unfinished